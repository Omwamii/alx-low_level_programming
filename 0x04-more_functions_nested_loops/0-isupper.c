#include "main.h"
#include <stdio.h>

/**
  *_isupper - checks if uppercase
  *@c: char to be checked
  *Return: 1 if uppercase, 0 if otherwise
  */
int _isupper(int c)
{
	if (c >= 'a' && c <= 'z')
		return (0)
	else if (c >= 'A' && c <= 'Z')
		return (1);
}
